import {schemaComposer} from "graphql-compose";

schemaComposer.createObjectTC({
    // name: "AccessToken",
    // fields: {
    //     accessToken: "String!"
    // },
});

schemaComposer.createObjectTC({
    // name: "Succeed",
    // fields: {succeed: "Boolean!"},
});